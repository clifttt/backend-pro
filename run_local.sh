#!/bin/bash
# Скрипт для запуска приложения локально (требует PostgreSQL)

echo "🚀 Investment Intelligence Hub - Local Launch"
echo "=============================================="

# Установка переменных окружения
export DATABASE_URL_LOCAL="postgresql+psycopg://postgres:postgres@localhost:5432/invest_db"

# Проверяем наличие Python
if ! command -v python3 &> /dev/null; then
    echo "❌ Python3 не найден. Пожалуйста, установите Python 3.10+"
    exit 1
fi

# Проверяем наличие виртуального окружения
if [ ! -d "venv" ]; then
    echo "📦 Создание виртуального окружения..."
    python3 -m venv venv
fi

# Активируем виртуальное окружение
echo "🔧 Активация виртуального окружения..."
source venv/bin/activate

# Установим зависимости
echo "📥 Установка зависимостей..."
pip install --upgrade pip
pip install -r requirements.txt

# Инициализируем БД
echo "💾 Инициализация БД..."
python init_db.py

# Запускаем приложение
echo "🌐 Запуск FastAPI сервера..."
echo "📍 Откройте браузер: http://localhost:8000"
echo "📚 API документация: http://localhost:8000/docs"
echo ""

uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
