# 📋 Руководство по реализации Collector Часть 2 + Нормализация

## 🎯 Обзор

Этот документ описывает реализацию всех требований для:
- **Collector Часть 2:** Обход блокировок, Scheduler, Обработка ошибок
- **Нормализация:** Очистка данных, Выделение сущностей, Оценка качества

## 📁 Новые файлы и компоненты

### 1. **enhanced_collector.py** - Улучшенный коллектор
Главные классы:
- `ProxyManager` - Ротация прокси-серверов
- `UserAgentManager` - Ротация User-Agent'ов
- `EnhancedDataCollector` - Основной коллектор с защитой от блокировок

**Ключевые функции:**
```python
# Обход блокировок
- Random User-Agents (fake-useragent)
- Proxy rotation
- Random delays (1-5 сек)
- Retry mechanism (max 3 попытки)

# Обработка ошибок
- Timeout handling
- Connection errors
- HTTP errors (429, 500+)
- Detailed logging
```

**Использование:**
```bash
python enhanced_collector.py
```

### 2. **normalizer.py** - Нормализация и обработка данных
Главные классы:
- `EntityExtractor` - Извлечение сущностей (даты, имена, числа)
- `DataNormalizer` - Нормализация всех полей
- `QualityAssessment` - Оценка качества данных (0-100 баллов)
- `DataProcessor` - Интеграция всех компонентов

**Нормализация:**
- Имена компаний (привести к Title Case, убрать спец. символы)
- Имена инвесторов (убрать LLC, Inc, Partners и т.д.)
- Типы раундов (SEED, SERIES A, SERIES B, etc.)
- Суммы финансирования (валидация диапазона)

**Выделение сущностей:**
- Даты: YYYY-MM-DD, MM/DD/YYYY, Full Format
- Имена: Простой паттерн для ФИ
- Числа: Финансовые суммы с множителями (M, B, K)

**Оценка качества (100 баллов):**
- +25: >=50 компаний
- +25: Целостность данных >=80%
- +25: 100% валидных финансовых сумм
- +25: Разнообразие (3+ типов раундов и инвесторов)

**Использование:**
```bash
python normalizer.py
```

### 3. **scheduler_main.py** - Автоматизация запуска
Главный класс:
- `CollectorScheduler` - Управление расписанием

**Расписание (настраивается):**
- Ежедневный сбор в 00:00 (полночь)
- Проверка здоровья каждый час
- Нормализация после каждого сбора

**Использование:**
```bash
python scheduler_main.py
```

Это запустит фоновый scheduler и первый цикл сбора прямо сейчас.

### 4. **test_integration.py** - Интегрированное тестирование
Тестирует все компоненты:
- Часть 1: Базовый сбор (50+ записей)
- Часть 2: Продвинутые функции
- Entity Extraction: Извлечение сущностей

**Использование:**
```bash
python test_integration.py
```

Генерирует `test_results.json` с полным отчетом.

## 🚀 Быстрый старт

### Шаг 1: Установить зависимости
```bash
pip install -r requirements.txt
```

**Новые пакеты в requirements.txt:**
- `apscheduler==3.10.4` - Scheduler
- `fake-useragent==1.4.0` - User-Agent ротация
- `beautifulsoup4==4.12.2` - Парсинг HTML
- `lxml==4.9.3` - XML/HTML обработка
- `retrying==1.3.3` - Retry механизм

### Шаг 2: Инициализировать БД
```bash
# Убедитесь что PostgreSQL запущен
# И DATABASE_URL правильно настроен в файлах

alembic upgrade head
```

### Шаг 3: Запустить тестирование
```bash
# Проверить что все компоненты работают
python test_integration.py
```

### Шаг 4: Запустить сбор данных
```bash
# Вариант A: Одноразовый запуск
python enhanced_collector.py
python normalizer.py

# Вариант B: Автоматический scheduler (рекомендуется)
python scheduler_main.py
```

## 📊 Результаты и Метрики

### После запуска enhanced_collector.py

**Логирование:**
```
2026-02-21 10:15:30 - INFO - 🚀 Начало сбора данных. Целевое количество: 55 записей
2026-02-21 10:15:31 - INFO - ⏳ Задержка: 2.34 сек
2026-02-21 10:15:33 - INFO - 🔄 Обработано 10 записей...
2026-02-21 10:15:45 - INFO - ✅ Сбор завершен! Всего записей в БД: 55
2026-02-21 10:15:45 - INFO - 🏥 Проверка здоровья коллектора...
2026-02-21 10:15:46 - INFO - ✅ Коллектор работает. Записей в БД: 55
```

### После запуска normalizer.py

**Отчет о качестве:**
```json
{
  "quality_score": 100,
  "total_companies": 55,
  "total_funding_rounds": 55,
  "total_investors": 55,
  "total_investments": 55,
  "companies_with_rounds": 55,
  "rounds_with_investments": 55,
  "valid_funding_amounts": 55,
  "unique_round_types": 4,
  "unique_investor_types": 4,
  "referential_integrity_percent": 100.0,
  "quality_checks": [
    "✅ Достаточное количество компаний (>50)",
    "✅ Хорошая целостность данных (100.0%)",
    "✅ Все суммы финансирования валидны",
    "✅ Хорошее разнообразие (Типов раундов: 4, Типов инвесторов: 4)"
  ],
  "assessment_timestamp": "2026-02-21T10:15:50.123456"
}
```

## 🔍 Проверка выполнения требований

### ✅ Collector Часть 1 (10%)
- [x] Базовый парсер существует (`collector.py`)
- [x] Сохранение в БД работает
- [x] 50+ записей в БД

### ✅ Collector Часть 2
- [x] **Обход блокировок:**
  - [x] User-Agents ротируются (`UserAgentManager.get_user_agent()`)
  - [x] Прокси ротируются (`ProxyManager.get_proxy()`)
  - [x] Задержки применяются (`_apply_delay()`, 1-5 секунд)

- [x] **Автоматизация:**
  - [x] APScheduler интегрирован
  - [x] Расписание по cron (ежедневно 00:00)
  - [x] Проверка здоровья (каждый час)
  - [x] Можно запустить вручную

- [x] **Обработка ошибок:**
  - [x] Retry механизм (до 3 попыток)
  - [x] Timeout обработка (10 сек)
  - [x] ConnectionError handling
  - [x] HTTPError handling (429, 500+)
  - [x] Логирование в `collector.log`

### ✅ Нормализация и БЛ
- [x] **Очистка данных:**
  - [x] Удаление лишних пробелов
  - [x] Нормализация регистра
  - [x] Удаление специальных символов

- [x] **Выделение сущностей:**
  - [x] Даты (3 формата)
  - [x] Имена (ФИ)
  - [x] Числа (финансовые суммы)

- [x] **Оценка качества:**
  - [x] Подсчет сущностей
  - [x] Проверка целостности FK
  - [x] Валидация данных
  - [x] Итоговый балл 0-100

## 🐛 Отладка и логирование

### Логи
- `collector.log` - Логи сбора данных
- `collector_scheduler.log` - Логи scheduler

### Команды отладки

```bash
# Просмотреть последние логи
tail -f collector.log
tail -f collector_scheduler.log

# Запустить тестирование
python test_integration.py

# Проверить количество записей в БД
python -c "
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from models import Company

engine = create_engine('postgresql://user:password@localhost:5432/investment_db')
Session = sessionmaker(bind=engine)
session = Session()
print(f'Компаний: {session.query(Company).count()}')
session.close()
"

# Просмотреть результаты оценки качества
python -c "
import json
with open('test_results.json') as f:
    results = json.load(f)
    print(json.dumps(results['tests'][1]['components'], indent=2))
"
```

## 📈 Следующие шаги (Неделя 4)

- [ ] Интеграция с FastAPI (app/main.py)
- [ ] REST API endpoints для собранных данных
- [ ] Веб-интерфейс для visualизации
- [ ] Отправка алертов при ошибках
- [ ] CI/CD на GitHub Actions
- [ ] Деплой на Docker

## 💡 Советы

1. **Для разработки:** Используйте одноразовый запуск
   ```bash
   python enhanced_collector.py
   python normalizer.py
   ```

2. **Для production:** Используйте scheduler
   ```bash
   python scheduler_main.py
   ```

3. **Для тестирования CI/CD:** Запустите интеграционные тесты
   ```bash
   python test_integration.py
   ```

4. **Для параллельного рабочего процесса с GitHub:** Используйте GitHub Actions в `.github/workflows/`

## 📞 Контакты и поддержка

- **Data Engineer:** Nurislam (Сбор и обработка данных)
- **Backend Developer:** Zhangir (REST API)
- **DevOps:** Nurym (Docker, CI/CD)
