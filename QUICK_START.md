# 🚀 QUICK START GUIDE

Это краткое руководство для быстрого запуска Investment Intelligence Hub API.

---

## ⚡ 5-минутный старт (Windows)

### 1️⃣ Требования
- Python 3.10+ (https://www.python.org/downloads/)
- PostgreSQL 12+ (https://www.postgresql.org/downloads/)

### 2️⃣ Клонируем проект

```bash
# Откройте командную строку в разделе backend-pro-main
cd backend-pro-main
```

### 3️⃣ Запускаем скрипт

```bash
# На Windows - двойной клик или:
run_local.bat

# На Linux/Mac:
bash run_local.sh
```

Это автоматически выполнит:
- ✅ Создание виртуального окружения
- ✅ Установку зависимостей
- ✅ Инициализацию БД
- ✅ Запуск FastAPI сервера

### 4️⃣ Откройте в браузере

- **API:** http://localhost:8000
- **Документация:** http://localhost:8000/docs ⭐ (самая интересная!)

---

## 🐍 Ручной запуск (Для опытных)

```bash
# 1. Активируем виртуальное окружение
python -m venv venv
venv\Scripts\activate  # Windows

# 2. Устанавливаем зависимости
pip install -r requirements.txt

# 3. Инициализируем БД
python init_db.py

# 4. Запускаем сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

---

## 🐳 Docker запуск

```bash
# Убедитесь что Docker и Docker Compose установлены

docker-compose up -d

# В другом терминале:
docker-compose exec web python init_db.py

# Сервер на http://localhost:8000
```

---

## 📝 Первый запрос API

Откройте браузер и перейдите на:

```
http://localhost:8000/docs
```

Вы увидите "Swagger UI" - интерактивное окружение для тестирования API.

### Попробуйте эти запросы:

**1. Получить список стартапов:**
```
GET /startups
```

**2. Поискать стартапы в США:**
```
GET /startups?country=USA
```

**3. Получить статистику:**
```
GET /statistics
```

**4. Проверить здоровье сервера:**
```
GET /health
```

---

## 🔗 Основные эндпоинты

| Метод | Эндпоинт | Описание |
|-------|----------|---------|
| GET | `/` | Главная страница |
| GET | `/health` | Проверка здоровья |
| GET | `/startups` | Список стартапов |
| GET | `/investors` | Список инвесторов |
| GET | `/investments` | Список инвестиций |
| GET | `/statistics` | Общая статистика |

---

## 🐛 Ошибки и решения

### ❌ "Connection refused" или PostgreSQL не работает

**Решение:**
```bash
# Убедитесь что PostgreSQL запущена
# На Windows: Services → PostgreSQL
# На Linux: sudo systemctl start postgresql
# На Mac: brew services start postgresql

# Или откройте pgAdmin и проверьте подключение
```

### ❌ "Port 8000 already in use" (порт занят)

**Решение:**
```bash
# Запустите на другом порту
uvicorn app.main:app --port 8001
```

### ❌ "ModuleNotFoundError"

**Решение:**
```bash
# Убедитесь что виртуальное окружение активировано
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# И переустановите зависимости
pip install -r requirements.txt
```

---

## 📚 Документация

Полная документация в файлах:
- **README.md** - Полный гайд
- **FINAL_COMPLETION_REPORT.md** - Отчет о завершении
- **IMPLEMENTATION_GUIDE.md** - Подробное описание

---

## 🎯 Что дальше?

1. **Сейчас:** Используйте `/docs` для изучения API
2. **Потом:** Прочитайте README.md для полной документации
3. **Интеграция:** Используйте API в своих приложениях

---

## 💬 Помощь

Если что-то не работает:
1. Проверьте что PostgreSQL запущена
2. Убедитесь что Python 3.10+
3. Прочитайте логи в консоли
4. Откройте GitHub Issues

---

**Счастливой кодинга! 🚀**
